1. clone git repository
2. create venv. run in terminal console:
   python -m venv venv
3. activate venv. run in terminal console:
   venv\Scripts\activate
4. install requirements. run in terminal console:
   pip install -r requirements.txt